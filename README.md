# Catalyst-IT CSV Parser

### Introdution

The CSV Parser is a Python script that converts a CSV file to a MySQL database.
user_upload.py takes a CSV file via Command Line Directives without extra 
configurations.

